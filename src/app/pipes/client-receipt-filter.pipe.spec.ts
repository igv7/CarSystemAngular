import { ClientReceiptFilterPipe } from './client-receipt-filter.pipe';

describe('ClientReceiptFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientReceiptFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
