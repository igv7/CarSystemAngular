import { ClientReceiptDateFilterPipe } from './client-receipt-date-filter.pipe';

describe('ClientReceiptDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientReceiptDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
